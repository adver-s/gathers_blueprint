データベースへの接続(Prisma Client)

目的: アプリが データベースにアクセスして CRUD（作成・読み取り・更新・削除）できるようにする
具体的にやること:
ユーザー情報やグループ情報を DB に保存する関数をまとめる
ポイント: Aurora/MySQL/PostgreSQL など DB が何であれ、アプリ側から扱う方法を統一することが目的。AWS SDK と混ぜないのは、DB と AWS サービスは責務が違うから。