API専用処理（DBラッパーやサーバ専用のバリデーションなど）

APIで使う便利関数を格納。

utils は 小さくて純粋で副作用のない、汎用関数だけ を置く場所。
✔ 特徴
アプリ固有の事情に依存しない
外部サービスを呼ばない
DB も触らない
再利用できる
✔ 例
文字列操作（capitalize, slugify）
配列処理（chunk, unique）
日付フォーマット
バリデーション関数
オブジェクトの deep clone