AWSの高度な処理をまとめる層。

AWS インフラサービス（S3 / Cognito / SES / Secrets Manager / SNS など）

目的: アプリが 作った AWS リソースを操作できるようにする
具体的にやること:
S3 にファイルをアップロードする処理をまとめる
Cognito でユーザー認証をする処理をまとめる
ポイント: ここにあるファイルは AWS の操作をする「窓口」 を定義するためのもの。リソースは Terraform で作ったものを想定して使う。